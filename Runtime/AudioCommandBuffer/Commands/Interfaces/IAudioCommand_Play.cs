namespace AudioTag {
	/// <summary>
	/// An audio command that signals that the <see cref="UnityEngine.AudioSource.clip"> is played.
	/// </summary>
	public interface IAudioCommand_Play : IAudioCommand { }
}